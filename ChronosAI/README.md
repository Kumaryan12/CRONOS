# Chronos AI

AI is an optional presentation layer over verified, aggregated statistics. The
always-on collector never invokes a model and raw activity history is never sent
to one by default. `RuleBasedInsightsProvider` is the V1 implementation; a future
`AIInsightsProvider` must accept summaries rather than raw events.
